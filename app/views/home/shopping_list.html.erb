<h1>General Shopping List</h1>
<p>Amount of food items to buy: <%= @foods.length%></p>
<p>Total value fo food needed: <%= @total_value%></p>
<table>
  <tr>
    <th>Food</th>
    <th>Quantity</th>
    <th>Price</th>
  </tr>
  <% @foods.each do |food| %>
    <tr>
      <td><%= food.name %></td>
      <td><%= food.quantity %> <%= food.measurment_unit %></td>
      <td>$ <%= food.price * food.quantity %></td>
    </tr>
  <% end %>
</table>
